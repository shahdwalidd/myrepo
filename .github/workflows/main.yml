on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      
      - name: Checkout code
        uses: actions/checkout@v3

     
      - name: Print something
        run: echo "shahd"
      - name: Print folder content
        run: ls

